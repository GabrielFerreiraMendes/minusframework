name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build Docusaurus
        run: npm run build
      - name: Deploy to GitHub Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git remote set-url origin "https://x-access-token:$GH_TOKEN@github.com/${{ github.repository }}"
          npx docusaurus deploy

  wiki:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Deploy Wiki
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if (Test-Path "deploy-wiki.ps1") {
            ./deploy-wiki.ps1 -Token "$env:GH_TOKEN"
          }

  release-sync:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Sync releases across submodules
        env:
          TOKEN: ${{ secrets.GH_PAT }}
        run: |
          $tag = "${{ github.ref_name }}"
          pwsh ./release-sync.ps1 -Version $tag -Token "$env:TOKEN"

  build:
    runs-on: [self-hosted, windows, delphi]
    if: false
    steps:
      - uses: actions/checkout@v5
      - name: Setup (clona os 9 repos)
        env:
          TOKEN: ${{ secrets.GH_PAT }}
        run: pwsh ./ci-setup.ps1 -Token "$env:TOKEN"
      - name: Build
        run: |
          & "C:\Program Files (x86)\Embarcadero\Studio\23.0\bin\bds.exe" `
            -p Win64 -r "MinusFramework_Runtime.dproj" -b `
            --Define:COMMUNITY
      - name: Run tests
        run: |
          Get-ChildItem -Recurse -Filter "run-tests.ps1" | ForEach-Object {
            & $_.FullName
          }
